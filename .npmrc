# NPM Configuration

# Ensure npm doesn't include unnecessary files
package-lock=true

# Set registry
registry=https://registry.npmjs.org/

# Ensure proper permissions for global installs
unsafe-perm=true

# Don't include npm debug logs in package
loglevel=warn